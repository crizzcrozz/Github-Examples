name: "Workflow Commands"

jobs:
  my-job: 
    runs-on: ubuntu-latest
    steps:
      - name: "group logging"
        run: |
          echo "::group::My Group Message"
          echo "MSG 1"
          echo "MSG 2"
          echo "::endgroup::"